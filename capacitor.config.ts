import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.initeractiveQuiz.apk',
  appName: 'Interactive Quiz',
  webDir: 'build'
};

export default config;
